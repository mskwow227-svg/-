# 2026 산림레포츠 축제 오리엔티어링 대회 — 랜딩페이지

부천 진달래동산에서 열리는 **2026 산림레포츠 축제 오리엔티어링 대회**(2026.10.17 토) 홍보/안내용 정적 웹페이지입니다.

- 참가비 무료, 선착순 500명
- 가족·청소년·청년 7개 클래스
- 클래스 추천, 시상 시각화, 스탬프 투어, 주차 요금 계산기, FAQ 검색

## 기술 구성

빌드 도구·프레임워크 없이 **순수 HTML / CSS / JS**로만 만들어졌습니다. 저장소를 그대로 정적 호스팅에 올리면 배포됩니다.

```
index.html              # 마크업
vercel.json             # Vercel 보안·캐시 헤더 (빌드 없음)
assets/
  css/styles.css        # 디자인 토큰(CSS 변수) + 컴포넌트 스타일
  js/config.js          # ★ 대회 데이터 전부 (여기만 고치면 됨)
  js/main.js            # 렌더링 · 상호작용 로직
  favicon.svg
```

## 콘텐츠 수정하기

일시, 장소, 신청 폼 URL, 전화번호, 클래스 정보, 주차 요금, 부스, 시상 내역 등
**대부분의 텍스트/숫자는 [`assets/js/config.js`](assets/js/config.js) 한 파일에서 수정**합니다.

| 바꾸고 싶은 것 | 위치 |
|---|---|
| 날짜·시간·장소·모집 인원·전화·구글폼 링크 | `config.js` → `OL.event` |
| **신청 마감일 / 모집중·마감 배지** | `config.js` → `OL.event.registration` (아래 참고) |
| 주차 요금 체계 | `config.js` → `OL.parking` |
| 클래스 7종 (이름·대상·방식·설명) | `config.js` → `OL.classes` |
| 클래스 찾기 위저드 옵션·매칭 규칙 | `config.js` → `OL.finder` |
| 시상 내역 (상 이름·팀 수) | `config.js` → `OL.awards` |
| 체험 부스 | `config.js` → `OL.booths` |
| FAQ 질문·답변 | `index.html` 의 `#faq-list` (검색이 DOM 텍스트 기반이라 마크업 유지) |
| 색상·간격·폰트 | `assets/css/styles.css` 상단 `:root` |

> `config.js` 의 `dateISO` 는 항상 `+09:00`(KST)을 붙여 주세요. 안 붙이면 보는 사람의 시간대에 따라 카운트다운이 어긋납니다.

### 모집중 / 마감 배지

`config.js` → `OL.event.registration`:

- `deadlineISO` / `deadlineLabel` : 신청 마감 일시 (현재 `2026-10-06T18:00:00+09:00`)
- `statusOverride` :
  - `'auto'` — 마감일이 지나면 히어로 배지가 자동으로 **"접수 마감"** 으로 바뀝니다 (기본값)
  - `'open'` — 마감일과 무관하게 계속 "모집 중"
  - `'closed'` — **선착순 조기 마감** 시 이 값으로 바꾸고 push 하면 즉시 마감 표시 + 마감 안내 문구 노출

마감 상태는 히어로 배지, 신청 안내 모달, 히어로 안내문에 동시에 반영됩니다.

### ⚠️ 개인정보·촬영 동의 안내 문안

`index.html` 의 `#privacy` 섹션 문안은 **일반 행사 기준 초안**입니다. 게시 전 부천여성청소년재단
개인정보 담당자가 ① 수집 항목을 구글폼과 일치, ② 보유기간·근거 법령, ③ 문의 담당자를
확정·검토해야 합니다. (해당 안내는 소스 코드 주석에도 표시해 두었습니다.)

## 로컬에서 미리보기

빌드가 필요 없습니다. `index.html` 을 브라우저로 바로 열면 됩니다.
(정적 서버를 쓰고 싶다면 VS Code "Live Server" 확장 등 아무거나 가능)

## 배포 (Vercel)

프로젝트 이름: **`echo-ol`** · 게시 주소: `https://echo-ol.vercel.app/`

최초 1회만 연결하면 이후 `main` 에 push할 때마다 자동 배포됩니다.

1. [vercel.com/new](https://vercel.com/new) → **Import Git Repository** 에서 `mskwow227-svg/-` 선택
2. **Project Name**: `echo-ol` (Vercel은 대문자를 허용하지 않아 소문자로)
3. **Framework Preset**: `Other` · **Root Directory**: `./` · Build/Output 설정은 비워 둠 (정적)
4. **Deploy**

`vercel.json` 에 보안 헤더와 캐시 정책(`max-age=0, must-revalidate`)이 정의돼 있습니다.
파일마다 ETag로 재검증하므로 push 후 내용이 **즉시** 반영됩니다(선착순 조기 마감 배지 전환 포함). 빌드 단계는 없습니다.

> 커스텀 도메인을 붙이면 `index.html` 의 `canonical`, `og:url` (10·19번째 줄 부근)을 그 주소로 바꿔 주세요.

### GitHub Pages 로도 띄우려면

`Settings → Pages → Deploy from a branch → main / (root)`. `assets/` 경로가 모두 상대경로라 하위 경로에서도 동작합니다.

## 앞으로 할 일 (콘텐츠·자산 필요)

이번 리팩토는 구조 분리와 접근성·SEO·성능·코드 안정성에 집중했습니다.
다음 항목은 **실제 자료가 준비되면** 추가하는 것이 좋습니다.

### 콘텐츠
- [ ] 행사/숲 분위기 **사진** 1~2장 (Hero, 부스 섹션)
- [ ] 주최·후원 **로고** 이미지 (푸터)
- [ ] **코스 지도** 이미지
- [ ] 당일 **타임테이블** (개회식·경기·시상 시간)
- [ ] 오시는 길 **지도 임베드** (카카오/구글 지도 또는 약도 이미지)
- [ ] **대표 이미지**(og:image, 1200×630) — 카톡 공유 썸네일용. `index.html` 의 OG 태그와 JSON-LD `image` 에 추가
- [ ] **개인정보 처리방침 / 초상권(현장 촬영) 안내**
- [ ] 문의 **이메일** 및 **카카오채널** (현재 전화번호만)

### 운영 기능
- [ ] 선착순 **잔여 인원 / 마감 배지** (정적 사이트라 수동 갱신)
- [ ] **공지사항 배너** 영역 (우천·변경 공지용)

### 디자인 심화
- [ ] 한글 웹폰트(Pretendard) 도입
- [ ] 이모지 아이콘 → 통일된 SVG 아이콘 세트
- [ ] Hero 우측 정보 카드 정보량 분리

### 아키텍처 (선택)
- [ ] 페이지가 늘어나면 Tailwind CLI 빌드 + GitHub Actions 배포로 전환

## 라이선스 / 문의

문의: 청소년활동팀 032-344-4480
